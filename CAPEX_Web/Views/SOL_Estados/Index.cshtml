@model IEnumerable<CAPEX_Web.SOL_Estados>

@{
    ViewBag.Title = "Estados";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ESTADO_MODULO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PROYECTO_STATUS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESTADO_PROXIMO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ESTADO_MODULO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PROYECTO_STATUS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESTADO_PROXIMO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ESTADO_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ESTADO_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ESTADO_ID })
        </td>
    </tr>
}

</table>
